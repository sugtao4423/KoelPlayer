<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Transition
        app:constraintSetEnd="@id/albumDetailCollapsed"
        app:constraintSetStart="@id/albumDetailExpanded">
        <OnSwipe
            app:dragDirection="dragUp"
            app:onTouchUp="autoComplete"
            app:touchAnchorId="@id/albumDetailMusicList"
            app:touchAnchorSide="top" />
    </Transition>

    <ConstraintSet android:id="@+id/albumDetailExpanded">
        <Constraint
            android:id="@+id/albumDetailToolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize">
            <CustomAttribute
                app:attributeName="background"
                app:customColorDrawableValue="@android:color/transparent" />
            <CustomAttribute
                app:attributeName="titleTextColor"
                app:customColorValue="@android:color/transparent" />
        </Constraint>
        <Constraint
            android:id="@+id/albumDetailCover"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
        <Constraint
            android:id="@+id/albumDetailTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/albumDetailCover" />
        <Constraint
            android:id="@+id/albumDetailArtist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/albumDetailTitle" />
    </ConstraintSet>

    <ConstraintSet android:id="@+id/albumDetailCollapsed">
        <Constraint
            android:id="@+id/albumDetailToolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize">
            <CustomAttribute
                app:attributeName="background"
                app:customColorDrawableValue="?attr/colorPrimary" />
            <CustomAttribute
                app:attributeName="titleTextColor"
                app:customColorValue="@android:color/white" />
        </Constraint>
        <Constraint
            android:id="@+id/albumDetailCover"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toTopOf="@id/albumDetailToolbar" />
        <Constraint
            android:id="@+id/albumDetailTitle"
            android:visibility="gone" />
        <Constraint
            android:id="@+id/albumDetailArtist"
            android:visibility="gone" />
        <Constraint
            android:id="@+id/albumDetailMusicList"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/albumDetailToolbar" />
    </ConstraintSet>

</MotionScene>